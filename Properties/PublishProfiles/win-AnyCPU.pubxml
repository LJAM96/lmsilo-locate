<?xml version="1.0" encoding="utf-8"?>
<!--
  Default publish profile placeholder so MSBuild can resolve the PublishProfile property
  during regular builds. Adjust settings as needed when creating packaged builds.
-->
<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <Platform>AnyCPU</Platform>
    <PublishDir>bin\Publish\AnyCPU\</PublishDir>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <PublishSingleFile>false</PublishSingleFile>
    <SelfContained>false</SelfContained>
  </PropertyGroup>
</Project>
